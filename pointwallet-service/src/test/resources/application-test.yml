spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:pointwallet-service;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop