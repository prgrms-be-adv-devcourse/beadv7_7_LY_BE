server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:33340/lp_auction
    username: root
    password: root

  jpa:
    hibernate:
      # core-service와 같은 DB(lp_auction)를 쓰므로, create로 두면 이쪽을 띄우는 것만으로
      # 상품·시세 데이터까지 전부 지워진다
      ddl-auto: none

app:
  jwt:
    secret-key: c2VjcmV0LWtleS1mb3ItbG9jYWwtZGV2ZWxvcG1lbnQtZW52aXJvbm1lbnQxMjM0NTY3ODkw
    token-expiration-time: 3600000  # 1시간 (밀리초 단위)
